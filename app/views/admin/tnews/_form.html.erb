<%= form_for(@admin_tnews) do |f| %>
  <% if @admin_tnews.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_tnews.errors.count, "error") %> prohibited this admin_tnews from being saved:</h2>

      <ul>
      <% @admin_tnews.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
